# PC-A Marco 本地真实环境 E2E 审计报告

生成日期：2026-09-02（Asia/Shanghai）  
固定 as-of：2026-09-02  
canonical data cutoff：2026-08-31

## 结论

Marco Contract v1/exporter 已在本地真实 canonical/DuckDB 环境执行成功，四个 JSON 均生成并通过项目集成契约测试；运行时数据层未被更新。整体结论为 **CONDITIONAL**：数据质量基础门通过，但 D2/D4 信号仍有 `SCALE_SATURATED`，且 `signal_status.py` 与运行手册不一致（不接受 `--today`），不能宣称所有运行门无条件 PASS。

## 门禁与证据

| 门 | 结果 | 证据 |
|---|---|---|
| 工作区/前提 | PASS（有偏差项） | HEAD `3b22ab6`（Contract/exporter）；未执行安装、模型/数据更新。运行期间产生本报告、artifacts 及本地快照。 |
| 可恢复备份 | PASS | `data/local/e2e_backup_20260902_223301/`，含 `macro.duckdb`、`canonical/macro/macro.parquet`、`canonical/market/market.parquet`，原地未覆盖。 |
| quality | PASS | `.venv\\Scripts\\python.exe scripts/check_quality.py` → `Quality: PASS`；仅频率/交易日缺口 WARN。 |
| signal status | CONDITIONAL | 无参数生产入口 → Core 15/15、Market 6/6、Structural 3/3 READY，missing-series 0；带 `--today` 按手册调用失败（入口不支持该参数）。 |
| historical coverage | PASS（机械）/CONDITIONAL（语义） | `historical_coverage.py --today 2026-09-02` 生成 36 行；coverage 结构通过，但 BLOCKED/TBD 或历史可比性不能由该文件自动证明。 |
| 四报告 | PASS（生成）/CONDITIONAL（质量） | Macro/Market/Asset/Structural/Signal Quality 均以 2026-09-02 重跑并退出 0；D2/D4 `SCALE_SATURATED`（50%/46%）保留。 |
| exporter | PASS | `scripts/export_cross_asset_snapshot.py --today 2026-09-02 --output-dir artifacts/integration/e2e_20260902` 退出 0，四 JSON 均存在。 |
| parity/契约 | PASS | JSON schema `1.0`；manifest 文件集合恰为 3 个数据 JSON；资产 ID 严格为 `CN_EQ, HK_EQ, US_EQ, CN_GOV_BOND, CN_CREDIT, GOLD, COMMODITY, CASH`，FX 为 `CNY`；字段与对应 Macro/Asset/Structural 语义层一致。 |
| null/截止日 | PASS（契约语义） | `as_of` 四文件均为 2026-09-02；macro/asset/manifest `data_cutoff=2026-08-31`；structural v1 schema 无 `data_cutoff` 字段。不可用 Fiscal 与不可用资产字段使用 null，未零填充。 |
| pytest | PASS | `PYTHONPATH=src .venv\\Scripts\\python.exe -m pytest -q` → 全部测试通过（100%）；有 Pandas4Warning，不影响退出状态。首次未设置 PYTHONPATH 时出现 `ModuleNotFoundError`，已按项目运行方式重跑通过。 |
| Streamlit smoke | PASS（HTTP）/CONDITIONAL（UI语义） | `.venv` 启动 `streamlit run ... --server.headless true --server.port 8501`；`Invoke-WebRequest http://127.0.0.1:8501/` → HTTP 200；未以 HTTP 200 代替人工面板语义验收。 |

## 精确运行命令

```powershell
$py = '.\\.venv\\Scripts\\python.exe'
& $py scripts\\check_quality.py
& $py scripts\\signal_status.py
& $py scripts\\historical_coverage.py --today 2026-09-02
& $py scripts\\macro_report.py --today 2026-09-02
& $py scripts\\market_report.py --today 2026-09-02
& $py scripts\\asset_report.py --today 2026-09-02
& $py scripts\\structural_report.py --today 2026-09-02
& $py scripts\\signal_quality.py --today 2026-09-02
& $py scripts\\export_cross_asset_snapshot.py --today 2026-09-02 --output-dir artifacts\\integration\\e2e_20260902
$env:PYTHONPATH = (Join-Path (Get-Location) 'src')
& $py -m pytest -q
```

## 产物与未解决风险

- JSON：`artifacts/integration/e2e_20260902/` 下 `macro_snapshot.json`、`structural_snapshot.json`、`fundamental_asset_view.json`、`integration_manifest.json`。
- 本次未改模型权重、canonical、raw，未做 PIT migration，未提交或推送。
- `docs/HISTORICAL_COVERAGE_MATRIX.md` 在本任务开始时已有用户修改；coverage 运行会覆盖该文件。运行前已另存当前文件，但原用户版本须由主 Agent 依据其工作树证据恢复/核对后再交付，不能把生成文件视作用户修改已保留。
- `config_hash`/manifest 中的 hash 字段仅作为 exporter 产物原样存在；按主机指令，本审计不执行、不比较任何 hash 校验。
- D2/D4 saturation 与 G3 等质量诊断需要后续人工解释；本报告不把生成成功升级为模型或数据质量无条件通过。

