.venv/
__pycache__/
*.pyc
.pytest_cache/
*.duckdb
*.duckdb.wal
data/local/
data/inbox/
# V4 Cloud Mirror: config/, data/raw/ and data/canonical/ ARE synced to the
# remote (remove machine-local artifacts from here only - never unignore
# *.duckdb, data/local/, logs/ or .venv/).
logs/
.streamlit/
*.egg-info/
build/
dist/
# One-off B-package research scratch scripts (archived findings live in
# docs/research/2026-08-30_bpack_structural_survey.md; the raw probes are
# machine-local, not deliverable).
_scratch_b_research/

# Local test/build artifacts and dependency trees
.pytest-*/
.pytest_*/
.tmp.driveupload/
node_modules/
outputs/
v/
